@model Country

<div class="row">
    <div class="col-1">@Model.CountryId</div>
    <div class="col-1">@Model.Name</div>
    <div class="col-1">@Html.ActionLink("Details","CountryDetails","Countries", new {id = Model.CountryId})</div>
</div>
