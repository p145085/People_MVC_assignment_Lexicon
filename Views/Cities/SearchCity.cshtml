@model List<City>
<h3>Search</h3>

<form method="get" action="@Url.Action("CitySearch", "Cities")">
    <label for="search">Search</label>
    <input type="search" name="search" id="search" />
    <button type="submit">Perform search</button>
</form>