@model Person

<h1>Details page</h1>

<div class="row">
    <div class="col-2">@Model.Id</div>
    <div class="col-2">@Model.FirstName</div>
    <div class="col-2">@Model.LastName</div>
    <div class="col-2">@Model.Age</div>
    <div class="col-2">@Model.City</div>
    <div class="col-2">@Model.Phone</div>
</div>

@*<dl>
    <dt>ID</dt>
    <dd>@Model.Id</dd>

    <dt>Name</dt>
    <dd>@Model.FullName</dd>
    <dd>@Model.FirstName @Model.LastName</dd>

    <dt>Age</dt>
    <dd>@Model.Age</dd>

    <dt>City of birth</dt>
    <dd>@Model.City</dd>

    <dt>Phone</dt>
    <dd>@Model.Phone</dd>
</dl>*@