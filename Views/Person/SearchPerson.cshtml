@model List<Person>;
<h3>Search</h3>

<div class="input-group">
    <div class="form-outline">
        <input class="form-control" type="search" name="idSearch" id="idInput" placeholder="Person ID" />
    </div>

    <button type="button" class="btn btn-primary" onclick="postWithAjax('@Url.Action("PersonDetails","Person")','idInput')">
        Search
    </button>
</div>


@*<form asp-action="SearchPerson" asp-controller="Person" method="post">
    <div asp-validation-summary="ModelOnly"></div>

    <h4>Search</h4>
    <input asp-for="SearchQuery" type="text" />
    <span asp-validation-for="SearchQuery"></span>

    <button type="submit" value="SearchPerson">Submit</button>
</form>*@

<div id="result"></div>

@* A form that allows you search the people in the table for whose names or cities
with name containing the string you entered in the form (PeopleService.Search).
*@